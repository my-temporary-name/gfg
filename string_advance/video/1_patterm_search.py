# Search the given string in main string / Pattern searching problem
# Example: txt: "GEEKSFORGEEKS" pat: "EKS" ; Output: 2

# Naive approach: TC=O((n-m+1)m)

# Rabin karp algorithm: TC=O(n-m+1)*m) but better than average case of naive approach

# KMP algorithm: TC=O(m)

# suffix tree: O(n) # for fixed text

